/**
 * Stage 5: PUBLISH
 * Upload videos to YouTube + social platforms
 *
 * Input:  output/video/ + output/reels/ + content/ (for descriptions)
 * Output: Published URLs
 */

// TODO: Implement (DEFERRED — manual upload initially)
// 1. Generate YouTube title + description from metadata
// 2. Upload full video to YouTube
// 3. Upload reels to YouTube Shorts / Facebook / Instagram
// 4. Log published URLs to metadata.json

export async function publish() {
  console.log("Stage 5: PUBLISH — deferred, manual upload for now");
}
